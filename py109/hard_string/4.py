# 4. Custom "starts with any" and "ends with any"
# -----------------------------------------------
# Implement two functions using ONLY basic string methods (no `startswith`/`endswith`):
#
#   starts_with_any(text, prefixes)
#   ends_with_any(text, suffixes)
#
# - `prefixes` and `suffixes` are lists of strings.
# - Return True if `text` starts with ANY of the prefixes, otherwise False.
# - Return True if `text` ends with ANY of the suffixes, otherwise False.
#
# Example:
#   starts_with_any("unbelievable", ["re", "un", "in"]) -> True
#   ends_with_any("archive.tar.gz", [".zip", ".gz"])    -> True
#
# Hint: You’ll need slicing and maybe `len`.
def starts_with_any(text, prefixes):
    pass

def ends_with_any(text, suffixes):
    pass
